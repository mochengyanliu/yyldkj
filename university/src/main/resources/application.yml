server:
  port: 8081

spring:
  datasource:
    username: yykjld
    password: LIjun520
    url: jdbc:mysql://localhost:3306/yykjld?useUnicode=true&characterEncoding=utf-8&useSSL=true&serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapper/*Mapper.xml
  type-aliases-package: com.technology.university.entity

#showSql
logging:
  level:
    com:
      technology:
        university:
         mapper: debug
